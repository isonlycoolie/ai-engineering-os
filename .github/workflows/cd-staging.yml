# CD stub: deploy to staging on merge to develop
# Configure secrets and deploy commands for your project.

name: CD - Deploy to Staging

on:
  push:
    branches: [develop]

jobs:
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4

      - name: Deploy placeholder
        run: |
          echo "Configure staging deployment (ECS, Kubernetes, etc.)"
          echo "See standards/git-workflow.md and workflows/stage-08-production-deployment.md"

      - name: Smoke tests placeholder
        run: echo "npm run test:smoke -- --env staging"

      - name: Notify on failure
        if: failure()
        run: echo "Wire Slack webhook for staging deploy failures"
